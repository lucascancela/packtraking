var bloques = [0,0,0,0,0,0];
var materias = ["mate", "mate", "lengua", "lengua","proy", "proy"];

for (let i = 0; i < materias.length; i++) {
    if(bloques[i]==0){
        
    }    
}
for (let i = 0; i < materias.length;) {
    materias.splice(0,1)
    materias
}
console.log(materias)
bloques[0] = materias[0]
console.log(bloques);


function solve(bloques, materias){


}